// Replace 'localhost' with your machine's local IP address (e.g., 192.168.1.5)
// to test on mobile devices connected to the same Wi-Fi.
const API_BASE_URL = 'http://localhost:5000';

export default API_BASE_URL;
